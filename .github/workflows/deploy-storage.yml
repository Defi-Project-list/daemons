name: Deploy Storage

on:
  push:
    branches:
      - master
    paths:
      - "storage/**"

jobs:
  build:
    runs-on: ubuntu-latest
    name: Test and Push
    steps:
      - name: git-checkout
        uses: actions/checkout@v2

      - name: Test
        run: |
          cd storage/
          npm install
          npm run test

      - name: Push
        uses: s0/git-publish-subdir-action@develop
        env:
          REPO: self
          BRANCH: deploy-storage
          FOLDER: storage
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          MESSAGE: "Storage tested and deployed"
